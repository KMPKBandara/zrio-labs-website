import {ArrowUpRight} from 'lucide-react'; import {Link} from 'react-router-dom'; import type {Service} from '../types';
export default function ServiceCard({service}:{service:Service}){const Icon=service.icon;return <article className="card group"><div className="mb-5 inline-flex rounded-xl bg-brand/10 p-3 text-cyan"><Icon/></div><h3 className="mb-3 text-xl font-semibold">{service.title}</h3><p className="muted mb-5">{service.short}</p><Link to={`/services/${service.slug}`} className="inline-flex items-center gap-2 text-sm font-semibold text-cyan">Learn more <ArrowUpRight size={17} className="transition-transform group-hover:translate-x-1"/></Link></article>}
