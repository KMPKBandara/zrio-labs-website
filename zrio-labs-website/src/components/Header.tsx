import {useState} from 'react'; import {Link,NavLink} from 'react-router-dom'; import {Menu,X} from 'lucide-react'; import {nav} from '../data/site';
export default function Header(){const [open,setOpen]=useState(false);return <header className="sticky top-0 z-50 border-b border-slate-800/80 bg-ink/90 backdrop-blur"><a href="#main" className="sr-only focus:not-sr-only">Skip to content</a><div className="container-pad flex h-20 items-center justify-between"><Link to="/" aria-label="Zrio Labs home" className="flex items-center gap-3"><img src="/brand/zrio-logo.png" alt="" className="h-12 w-12 rounded-lg object-cover"/><span className="text-xl font-bold">Zrio <span className="text-brand">Labs</span></span></Link><nav className="hidden items-center gap-7 md:flex" aria-label="Main navigation">{nav.map(([n,p])=><NavLink key={p} to={p} className={({isActive})=>`text-sm font-medium ${isActive?'text-cyan':'text-slate-300 hover:text-white'}`}>{n}</NavLink>)}<Link className="btn-primary" to="/contact">Start a Project</Link></nav><button className="p-2 md:hidden" aria-label="Toggle menu" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="container-pad flex flex-col gap-4 border-t border-slate-800 py-5 md:hidden">{nav.map(([n,p])=><Link key={p} to={p} onClick={()=>setOpen(false)}>{n}</Link>)}<Link className="btn-primary" to="/contact" onClick={()=>setOpen(false)}>Start a Project</Link></nav>}</header>}
