import {Navigate,useParams,Link} from 'react-router-dom'; import {CheckCircle2,ArrowLeft} from 'lucide-react'; import SEO from '../components/SEO'; import CTA from '../components/CTA'; import {services} from '../data/services';
export default function ServiceDetail(){const {slug}=useParams();const s=services.find(x=>x.slug===slug);if(!s)return <Navigate to="/404" replace/>;const Icon=s.icon;return <><SEO title={`${s.title} | Zrio Labs`} description={s.short}/><section className="section"><div className="container-pad"><Link to="/services" className="mb-10 inline-flex gap-2 text-sm text-cyan"><ArrowLeft size={18}/> All services</Link><div className="grid gap-12 lg:grid-cols-2"><div><div className="mb-5 inline-flex rounded-xl bg-brand/10 p-4 text-cyan"><Icon size={32}/></div><h1 className="title mb-6">{s.title}</h1><p className="muted text-lg">{s.description}</p><Link to="/contact" className="btn-primary mt-8">Discuss your project</Link></div><div className="card"><h2 className="mb-6 text-2xl font-bold">What we can help with</h2><ul className="space-y-4">{s.features.map(f=><li key={f} className="flex gap-3"><CheckCircle2 className="shrink-0 text-cyan" size={20}/>{f}</li>)}</ul></div></div></div></section><CTA/></>}
